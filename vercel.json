{
  "version": 2,
  "builds": [
    { "src": "/package.json", "use": "@vercel/next" }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://REPLACE_WITH_BACKEND.example.com/api/$1"
    }
  ]
}
